import { ApiService } from './ApiService';

describe('ApiService', () => {
    // beforeAll(() => {
    //     jest.useFakeTimers();
    //     jest.spyOn(global, 'setTimeout');
    // });
    // afterAll(() => {
    //     jest.useRealTimers();
    //     jest.clearAllTimers();
    // });
    it('should test ApiService', () => {

        // expect(true).toBe(true);

        // const spy = jest
        //     .spyOn(ApiService, ApiService.test.name as keyof typeof ApiService);
        //     .mockReturnValue(123);
        // expect(spy).toBeCalledTimes(1);

        // const mockedApiService = jest.mocked(ApiService);
        // mockedApiService.test.mockReturnValue(456);

    });
});