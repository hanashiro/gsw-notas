import { BankPageLogic } from './Bank.logic';

describe('BankPageLogic', () => {

    // beforeAll(() => {
    //     jest.useFakeTimers();
    //     jest.spyOn(global, 'setTimeout');
    // });
    // afterAll(() => {
    //     jest.useRealTimers();
    //     jest.clearAllTimers();
    // });
    it('should test BankPageLogic', () => {

        // expect(true).toBe(true);

        // const spy = jest
        //     .spyOn(BankPageLogic, BankPageLogic.test.name as keyof typeof BankPageLogic);
        //     .mockReturnValue(123);
        // expect(spy).toBeCalledTimes(1);

        // const mockedBankPageLogic = jest.mocked(BankPageLogic);
        // mockedBankPageLogic.test.mockReturnValue(456);

    });
});
